# Pusher Configuration
# Sign up for free at https://pusher.com
# These values are optional - the app will work in local mode without them
# For production multiplayer, you need to set these

PUSHER_APP_ID=your_app_id
PUSHER_SECRET=your_secret_key
NEXT_PUBLIC_PUSHER_KEY=your_public_key
NEXT_PUBLIC_PUSHER_CLUSTER=us2
